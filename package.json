{
  "name": "@namefi/dnssec-monorepo",
  "version": "2.1.0",
  "private": true,
  "description": "Monorepo for @namefi/dnssec-audit, the DNSSEC chain-of-trust webapp, and the Chrome extension",
  "license": "MIT",
  "workspaces": [
    "packages/dnssec-audit",
    "packages/shared",
    "packages/webapp",
    "packages/chrome-extension"
  ],
  "scripts": {
    "build": "npm run build -w packages/dnssec-audit && npm run build -w packages/shared && npm run build -w packages/webapp && npm run build -w packages/chrome-extension",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test -w packages/dnssec-audit",
    "dev:webapp": "npm run dev -w packages/webapp",
    "dev:chrome-extension": "npm run dev -w packages/chrome-extension",
    "export": "npm run export -w packages/dnssec-audit --",
    "validate": "npm run validate -w packages/dnssec-audit --"
  },
  "engines": {
    "node": ">=22.6.0"
  }
}
